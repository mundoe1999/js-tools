{
  "name": "json-to-csv",
  "version": "1.0.0",
  "description": "Converts flat JSON file to a readable CSV file (using | separators). Expects a List of JSON object with all the keys on the first entry",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
